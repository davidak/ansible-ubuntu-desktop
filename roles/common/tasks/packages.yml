- name: install basic packages
  apt:
    name: "{{ item }}"
    update_cache: yes
  with_items:
    - htop
    - vim
    - screen
    - git
    - ssh
    - sshfs
    - curl
    - whois
    - unzip
    - tree
    - seahorse
    - virtualbox-qt
    - remmina
